server:
  port: 0
spring:
  application:
    name: BILLING-SERVICE
  datasource:
    url: ${BILLING_DB_URL:jdbc:mysql://localhost:3306/db-vscm?createDatabaseIfNotExist=true&serverTimezone=UTC}
    username: ${BILLING_DB_USER:root}
    password: ${BILLING_DB_PASSWORD:mysql}
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
