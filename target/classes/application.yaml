server:
  port: 9002
spring:
  application:
    name: CONFIG-SERVER
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-locations:
            - classpath:/configurations/platform
            - classpath:/configurations/services
management:
  endpoints:
    web:
      exposure:
        include: health,info
